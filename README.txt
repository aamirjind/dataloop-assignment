Assignment contains following tasks covered.

1. loads a dictionary and can save to dictionary: `to_dict`, `from_dict`
2. ability to instantiate directly from the class
3. default value: can define a default value for attributes
4. autocomplete : It picks first key that matches initial form dictionary no matter at what depth it is
5. dynamic and general usability for the class (easily define new data structures): Just like in given example dictionary named data you can instantiate any data structure with x-level of depth to define class _data object 
6. ability to reflect inner value on the main level